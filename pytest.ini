[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 覆盖率配置
addopts = --tb=short -q

# 测试标记
markers =
    unit: 单元测试
    integration: 集成测试
    slow: 慢速测试（需要LLM调用）
    skip_llm: 跳过需要LLM的测试
