Manual and exploratory test scripts live here.

These files are intentionally kept outside the main `pytest` suite declared in `pytest.ini`.
Use them for ad hoc verification, debugging, or migration checks, and promote stable coverage into `tests/unit` or `tests/integration` when appropriate.
